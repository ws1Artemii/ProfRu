<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProfRu" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/stroke_color</item>
        <item name="colorPrimaryVariant">@color/stroke_color</item>
        <item name="colorOnPrimary">@color/stroke_color</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/stroke_color</item>
        <item name="colorSecondaryVariant">@color/stroke_color</item>
        <item name="colorOnSecondary">@color/stroke_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="RoundShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%p</item>
    </style>

    <style name="BoxShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">11dp</item>
    </style>
</resources>